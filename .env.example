# Configurações da Aplicação
NODE_ENV=development
PORT=3000

# JWT Secret (gerar uma string aleatória em produção)
JWT_SECRET=seu_secret_jwt_aqui_mude_em_producao

# MongoDB (exemplo de conexão)
MONGODB_URI=mongodb://localhost:27017/projectnode

# PostgreSQL (exemplo de conexão)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=projectnode
POSTGRES_USER=postgres
POSTGRES_PASSWORD=senha_aqui
